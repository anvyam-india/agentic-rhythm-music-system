export const chartColors = {
  primary: 'var(--chart-1)',
  secondary: 'var(--chart-2)',
  tertiary: 'var(--chart-3)',
  quaternary: 'var(--chart-4)',
  accent: 'var(--accent)',
  border: 'var(--border)',
  text: 'var(--text-secondary)',
  grid: 'var(--border)',
} as const

export const defaultMargin = { top: 8, right: 8, left: 0, bottom: 0 }
